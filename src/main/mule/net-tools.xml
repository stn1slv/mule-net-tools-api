<?xml version="1.0" encoding="UTF-8"?>
<mule xmlns:ee="http://www.mulesoft.org/schema/mule/ee/core"
	xmlns:doc="http://www.mulesoft.org/schema/mule/documentation"
	xmlns="http://www.mulesoft.org/schema/mule/core"
	xmlns:apikit="http://www.mulesoft.org/schema/mule/mule-apikit"
	xmlns:http="http://www.mulesoft.org/schema/mule/http"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd http://www.mulesoft.org/schema/mule/http http://www.mulesoft.org/schema/mule/http/current/mule-http.xsd http://www.mulesoft.org/schema/mule/mule-apikit http://www.mulesoft.org/schema/mule/mule-apikit/current/mule-apikit.xsd
http://www.mulesoft.org/schema/mule/ee/core http://www.mulesoft.org/schema/mule/ee/core/current/mule-ee.xsd">

	<flow name="net-tools-main"
		doc:id="6f86a93f-9de9-4cf8-b092-b6a4b23c42f4">
		<!-- outputMimeType pins the media type of the *inbound* payload. Without it the
		     listener tags the body with the request's own Content-Type, and DataWeave then
		     resolves `payload` to a parsed Object for any type it has a reader for, so
		     read(payload, "application/octet-stream") in curl-request fails outright for
		     application/json and application/xml. Verified against a standalone DataWeave
		     engine: read() is byte-exact at application/octet-stream and text/plain, and
		     throws UnexpectedFunctionCallTypesException once the payload carries a
		     parseable media type. The caller's real Content-Type is still available, and
		     is read from attributes.headers["content-type"] for forwarding. -->
		<http:listener config-ref="https" path="/api/*" outputMimeType="application/octet-stream">
			<http:response
				statusCode="#[vars.httpStatus default 200]">
				<http:headers>#[vars.outboundHeaders default {}]</http:headers>
			</http:response>
			<http:error-response
				statusCode="#[vars.httpStatus default 500]">
				<http:body>#[payload]</http:body>
				<http:headers>#[vars.outboundHeaders default {}]</http:headers>
			</http:error-response>
		</http:listener>
		<http:basic-security-filter doc:name="Basic security filter" realm="mule" />
		<!-- Path only, and deliberately not the target. Since 3.0 the curl parameters
		     arrive as x-target-* request headers rather than query parameters, so the
		     query string is no longer the danger it was. The danger moved rather than
		     disappeared: x-target-credentials carries credentials, x-target-header
		     carries Authorization, and x-target-url can embed credentials as
		     https://user:pass@host. None of them are logged. Do not "restore" target
		     logging on the grounds that the query string is now empty.

		     rawRequestPath rather than requestPath, because requestPath is URL-decoded, so
		     a request to /api/%0aINFO%20forged would decode into a real newline and let the
		     caller write a forged line into the log. rawRequestPath is the path encoded as
		     received (HTTP connector 1.5.0 and later), so %0a stays literal. -->
		<logger level="INFO"
			message='#["$(attributes.scheme default "") $(attributes.method default "") $(attributes.rawRequestPath default "")"]'
			doc:name="Log Endpoint" />
		<apikit:router config-ref="net-tools-config" />
        <error-handler>
            <on-error-propagate type="APIKIT:BAD_REQUEST">
                <ee:transform xmlns:ee="http://www.mulesoft.org/schema/mule/ee/core" xsi:schemaLocation="http://www.mulesoft.org/schema/mule/ee/core http://www.mulesoft.org/schema/mule/ee/core/current/mule-ee.xsd">
                    <ee:message>
                        <ee:set-payload><![CDATA[%dw 2.0
output application/json
---
{message: "Bad request"}]]></ee:set-payload>
                    </ee:message>
                    <ee:variables>
                        <ee:set-variable variableName="httpStatus">400</ee:set-variable>
                    </ee:variables>
                </ee:transform>
            </on-error-propagate>
            <on-error-propagate type="APIKIT:NOT_FOUND">
                <ee:transform xmlns:ee="http://www.mulesoft.org/schema/mule/ee/core" xsi:schemaLocation="http://www.mulesoft.org/schema/mule/ee/core http://www.mulesoft.org/schema/mule/ee/core/current/mule-ee.xsd">
                    <ee:message>
                        <ee:set-payload><![CDATA[%dw 2.0
output application/json
---
{message: "Resource not found"}]]></ee:set-payload>
                    </ee:message>
                    <ee:variables>
                        <ee:set-variable variableName="httpStatus">404</ee:set-variable>
                    </ee:variables>
                </ee:transform>
            </on-error-propagate>
            <on-error-propagate type="APIKIT:METHOD_NOT_ALLOWED">
                <ee:transform xmlns:ee="http://www.mulesoft.org/schema/mule/ee/core" xsi:schemaLocation="http://www.mulesoft.org/schema/mule/ee/core http://www.mulesoft.org/schema/mule/ee/core/current/mule-ee.xsd">
                    <ee:message>
                        <ee:set-payload><![CDATA[%dw 2.0
output application/json
---
{message: "Method not allowed"}]]></ee:set-payload>
                    </ee:message>
                    <ee:variables>
                        <ee:set-variable variableName="httpStatus">405</ee:set-variable>
                    </ee:variables>
                </ee:transform>
            </on-error-propagate>
            <on-error-propagate type="APIKIT:NOT_ACCEPTABLE">
                <ee:transform xmlns:ee="http://www.mulesoft.org/schema/mule/ee/core" xsi:schemaLocation="http://www.mulesoft.org/schema/mule/ee/core http://www.mulesoft.org/schema/mule/ee/core/current/mule-ee.xsd">
                    <ee:message>
                        <ee:set-payload><![CDATA[%dw 2.0
output application/json
---
{message: "Not acceptable"}]]></ee:set-payload>
                    </ee:message>
                    <ee:variables>
                        <ee:set-variable variableName="httpStatus">406</ee:set-variable>
                    </ee:variables>
                </ee:transform>
            </on-error-propagate>
            <on-error-propagate type="APIKIT:UNSUPPORTED_MEDIA_TYPE">
                <ee:transform xmlns:ee="http://www.mulesoft.org/schema/mule/ee/core" xsi:schemaLocation="http://www.mulesoft.org/schema/mule/ee/core http://www.mulesoft.org/schema/mule/ee/core/current/mule-ee.xsd">
                    <ee:message>
                        <ee:set-payload><![CDATA[%dw 2.0
output application/json
---
{message: "Unsupported media type"}]]></ee:set-payload>
                    </ee:message>
                    <ee:variables>
                        <ee:set-variable variableName="httpStatus">415</ee:set-variable>
                    </ee:variables>
                </ee:transform>
            </on-error-propagate>
            <on-error-propagate type="APIKIT:NOT_IMPLEMENTED">
                <ee:transform xmlns:ee="http://www.mulesoft.org/schema/mule/ee/core" xsi:schemaLocation="http://www.mulesoft.org/schema/mule/ee/core http://www.mulesoft.org/schema/mule/ee/core/current/mule-ee.xsd">
                    <ee:message>
                        <ee:set-payload><![CDATA[%dw 2.0
output application/json
---
{message: "Not Implemented"}]]></ee:set-payload>
                    </ee:message>
                    <ee:variables>
                        <ee:set-variable variableName="httpStatus">501</ee:set-variable>
                    </ee:variables>
                </ee:transform>
            </on-error-propagate>
        </error-handler>
    </flow>
	<!-- One flow per verb, and each does nothing but delegate. The method this API is
	     called with is the method the target receives, so there is no method parameter
	     to validate: APIkit answers 405 for anything not declared in the RAML. All the
	     logic lives in curl-request, which holds the only DataWeave-to-Java call. -->
	<flow name="get:\curl:net-tools-config">
		<flow-ref name="curl-request" doc:name="curl-request" />
    </flow>
	<flow name="post:\curl:net-tools-config">
		<flow-ref name="curl-request" doc:name="curl-request" />
    </flow>
	<flow name="put:\curl:net-tools-config">
		<flow-ref name="curl-request" doc:name="curl-request" />
    </flow>
	<flow name="patch:\curl:net-tools-config">
		<flow-ref name="curl-request" doc:name="curl-request" />
    </flow>
	<flow name="delete:\curl:net-tools-config">
		<flow-ref name="curl-request" doc:name="curl-request" />
    </flow>
	<sub-flow name="curl-request">
		<!-- Refuse an oversized body before anything materialises it. read() below pulls
		     the whole request into heap, and 3.0 widened what can get here: 2.x bounded
		     this incidentally, because only three media types were accepted and a GET
		     discarded its body, whereas now every verb relays a body of any type. The
		     limit matches the 10 MiB cap that max-filesize puts on the response side,
		     which exists for the same reason.

		     This reads Content-Length rather than the payload, so nothing is buffered to
		     decide. A chunked request declares no length and so is not covered; that is a
		     known gap, and closing it would mean streaming the body to curl's stdin
		     rather than passing a byte[]. -->
		<choice doc:name="Body within the size limit?">
			<!-- Both operands carry their own `default`, so neither depends on `and`
			     short-circuiting. The regex test alone would be enough today, but a
			     reordering or a "simplification" of it would otherwise turn every request
			     without a Content-Length into a coercion error rather than a 500-free
			     pass-through. `matches` is a full match, so a malformed length falls
			     through to <otherwise>; the connector rejects those upstream anyway. -->
			<when expression='#[((attributes.headers["content-length"] default "") matches /^[0-9]+$/) and (((attributes.headers["content-length"] default "0") as Number) > 10485760)]'>
				<ee:transform doc:name="Refuse oversized body">
					<ee:message>
						<ee:set-payload><![CDATA[%dw 2.0
output text/plain
---
"Request body exceeds the 10485760 byte limit."]]></ee:set-payload>
					</ee:message>
					<ee:variables>
						<ee:set-variable variableName="httpStatus">413</ee:set-variable>
					</ee:variables>
				</ee:transform>
			</when>
			<otherwise>
				<ee:transform doc:name="Run curl" doc:id="c96eaddb-f56e-4f1a-81ff-ec15df7cb6d6">
					<ee:message>
						<ee:set-payload><![CDATA[%dw 2.0
output application/java
import java!com::mulesoft::tool::network::NetworkUtils
//  read(), never write(). write() parses the payload and serialises it again, which
//  is what used to alter JSON and XML bodies on the way through.
//
//  This only works because the listener pins the inbound payload to
//  application/octet-stream. Left with the request's own Content-Type, DataWeave
//  resolves `payload` to a parsed Object for any format it has a reader for, and
//  read() then fails outright for application/json and application/xml.
//
//  Header names are lowercase because the HTTP connector normalises inbound names
//  to lowercase before they reach attributes.headers. Callers may use any case.
//
//  Target headers arrive one per inbound header, as x-target-h-<name>: <value>,
//  and each is rebuilt into the "name: value" form curl's -H expects. They are not
//  a repeated x-target-header: repeated header names were verified to be folded to
//  a single value somewhere between the caller and this flow, so only the last
//  survived and every earlier one was silently lost. One header name per target
//  header cannot be folded. 11 is the length of the "x-target-h-" prefix, and the
//  size check drops a bare "x-target-h-" that would otherwise send a nameless
//  header.
var targetHeaders = (attributes.headers default {})
  filterObject ((v, k) -> ((k as String) startsWith "x-target-h-") and (sizeOf(k as String) > 11))
  pluck ((v, k) -> ((k as String)[11 to -1]) ++ ": " ++ (v as String))
---
NetworkUtils::curl(
  attributes.headers["x-target-url"] default "",
  attributes.method,
  if (payload == null) null else read(payload, "application/octet-stream"),
  targetHeaders default [],
  (attributes.headers["x-target-insecure"] default "false") as Boolean,
  attributes.headers["x-target-credentials"] default "",
  attributes.headers["x-target-auth-type"] default "basic",
  attributes.headers["content-type"] default ""
)]]></ee:set-payload>
					</ee:message>
				</ee:transform>
			</otherwise>
		</choice>
    </sub-flow>
    <flow name="get:\dns:net-tools-config">
        <ee:transform xmlns:ee="http://www.mulesoft.org/schema/mule/ee/core" xsi:schemaLocation="http://www.mulesoft.org/schema/mule/ee/core http://www.mulesoft.org/schema/mule/ee/core/current/mule-ee.xsd" doc:id="2eb37294-21d7-412a-8ab6-8da73008b111">
            <ee:message>
                <ee:set-payload><![CDATA[%dw 2.0
output application/java
import java!com::mulesoft::tool::network::NetworkUtils
---
NetworkUtils::resolveIPs(attributes.queryParams.host, attributes.queryParams.dnsServer default "")]]></ee:set-payload>
            </ee:message>
        </ee:transform>
    </flow>
    <flow name="get:\ping:net-tools-config">
		<ee:transform doc:name="Transform Message" doc:id="4042b6d3-cc6a-4fae-ae4c-ea39b6650dbe" >
			<ee:message >
				<ee:set-payload ><![CDATA[%dw 2.0
output application/java
import java!com::mulesoft::tool::network::NetworkUtils
---
NetworkUtils::ping(attributes.queryParams.host)]]></ee:set-payload>
			</ee:message>
		</ee:transform>
    </flow>
    <flow name="get:\socket:net-tools-config">
        <ee:transform xmlns:ee="http://www.mulesoft.org/schema/mule/ee/core" xsi:schemaLocation="http://www.mulesoft.org/schema/mule/ee/core http://www.mulesoft.org/schema/mule/ee/core/current/mule-ee.xsd" doc:id="38147a29-0fe5-47f2-ba74-135f9392ecd3">
            <ee:message>
                <ee:set-payload><![CDATA[%dw 2.0
output application/java
import java!com::mulesoft::tool::network::NetworkUtils
---
NetworkUtils::testConnect(attributes.queryParams.host, attributes.queryParams.port)]]></ee:set-payload>
            </ee:message>
        </ee:transform>
    </flow>
    <flow name="get:\certest:net-tools-config" doc:id="8e725dad-59c4-4713-9771-91a850e9ce9d" >
		<ee:transform doc:name="Transform Message" doc:id="70af9c23-b034-40fc-ab42-7781c55b85e3" >
			<ee:message >
				<ee:set-payload ><![CDATA[%dw 2.0
output application/java
import java!com::mulesoft::tool::network::NetworkUtils
---
NetworkUtils::certest(attributes.queryParams.host, attributes.queryParams.port)]]></ee:set-payload>
			</ee:message>
		</ee:transform>
	</flow>
	<flow name="get:\ciphertest:net-tools-config" doc:id="349ff8fc-6032-4d79-85c9-5d0c4a59e035" >
		<ee:transform doc:name="Transform Message" doc:id="690960a8-0545-4d52-ab77-c170ff06a27d" >
			<ee:message >
				<ee:set-payload ><![CDATA[%dw 2.0
output application/java
import java!com::mulesoft::tool::network::NetworkUtils
---
NetworkUtils::cipherTest(attributes.queryParams.host, attributes.queryParams.port)]]></ee:set-payload>
			</ee:message>
		</ee:transform>
	</flow>
	<flow name="get:\diagnostics:net-tools-config">
		<ee:transform doc:name="Transform Message">
			<ee:message>
				<ee:set-payload><![CDATA[%dw 2.0
output application/java
import java!com::mulesoft::tool::network::NetworkUtils
---
NetworkUtils::curlVersion()]]></ee:set-payload>
			</ee:message>
		</ee:transform>
    </flow>
	<flow name="get:\traceroute:net-tools-config">
        <ee:transform xmlns:ee="http://www.mulesoft.org/schema/mule/ee/core" xsi:schemaLocation="
http://www.mulesoft.org/schema/mule/http http://www.mulesoft.org/schema/mule/http/current/mule-http.xsd 
http://www.mulesoft.org/schema/mule/scripting http://www.mulesoft.org/schema/mule/scripting/current/mule-scripting.xsd http://www.mulesoft.org/schema/mule/ee/core http://www.mulesoft.org/schema/mule/ee/core/current/mule-ee.xsd" doc:id="a006967e-8161-45f9-af3b-d1f3386ae267">
            <ee:message>
                <ee:set-payload><![CDATA[%dw 2.0
output application/java
import java!com::mulesoft::tool::network::NetworkUtils
---
NetworkUtils::traceRoute(attributes.queryParams.host)]]></ee:set-payload>
            </ee:message>
        </ee:transform>
    </flow>
</mule>
